# Made by Cristian Fildan
import pm4py

#method that transofrms a XES file to a CSV file
def XES_to_CSV(xes_file):
    log = pm4py.read_xes(xes_file)
    pd = pm4py.convert_to_dataframe(log)
    save = pd.to_csv(xes_file[:-4] + '.csv', index=False)



XES_to_CSV("BPI_Challenge_2012.xes")
